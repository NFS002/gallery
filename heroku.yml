setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
build:
  docker:
    gallery: Dockerfile
release:
  image: gallery
  command:
    - /bin/app migrate; /bin/app task db:seed
run:
  gallery: /bin/app